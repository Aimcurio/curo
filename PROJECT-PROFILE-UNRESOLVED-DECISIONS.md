# Curo Project-Profile Unresolved Decisions

Status: `DECISION_REQUIRED`

No item below was guessed or silently approved. The profiles and assignments
remain proposals until the human requester records decisions and the runtime
revalidates their hashes and constraints.

## KAYO

1. Confirm the current external KAYO project root.
2. Select capability-compatible provider/model identities for Strategist,
   Builder, and Critic/Reviewer, or explicitly retain `UNKNOWN`.
3. Verify and approve the local/private model runtime, transport, storage, and
   boundary for confidential or restricted context.
4. Approve the KAYO-specific deterministic validator implementation and command
   set.
5. Define which Second Brain context may be retrieved and which mutations, if
   any, may occur after separate human authorization.
6. Record an approval reference for the reviewed profile and assignment set.

KAYO approval must not collapse these boundaries: Second Brain owns knowledge;
KAYO coordinates; Control Room observes; Visual Data Node supports
understanding; Executor acts; Validator establishes evidence; Human authorizes.

## Spacetime

1. Confirm that `E:\spacetime`, recorded on 2026-09-05, is the current project
   root and identify the currently authorized work.
2. Select and approve providers/models for Builder and Critic/Reviewer.
3. Approve the current deterministic build and behavior validation commands.
4. Decide the current lifecycle status; the historical `COMPLETED` claim is not
   treated as current validation.
5. Record an approval reference for the profile and assignments.

## Math Node

1. Identify and approve the current Math Node source root.
2. Select providers/models for Builder and Critic/Reviewer, if model assistance
   is desired.
3. Verify and approve the deterministic validator implementation, semantic
   contracts, and command set at the current root.
4. Decide the current lifecycle status and authorized work.
5. Record an approval reference for the profile and assignments.

## PDF learning proposals

1. Independently validate the proposed skill and anti-pattern changes.
2. Review each candidate separately through HITL.
3. Approve, reject, or revise each candidate independently.
4. Promote only after the canonical learning protocol's evidence and validation
   gates pass; the historical combined candidate confers no approval.

## Release boundary

The project-profile and assignment contracts may be release-significant, but
this migration does not authorize a version bump, commit, tag, push, or release.
The lead coordinator must present the consolidated release recommendation after
the full regression and independent-review gates.
