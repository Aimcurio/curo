# Curo Run Index

Use one row per governed run. Keep this index concise; detailed evidence lives
in the harness-owned evidence location.

| Date/time | Run ID | Project | Workflow | Role | Result | Evidence locator | Current task |
|---|---|---|---|---|---|---|---|
| `<ISO-8601>` | `<run_id>` | `<project_id>` | `<workflow_id>` | `<role>` | `<status>` | `<locator>` | `<one immediate task>` |

Do not treat a missing row, timeout, silence, or unavailable evidence as
approval. Preserve historical rows and record corrections explicitly.
